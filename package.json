{
  "name": "ui-theme-config-api",
  "version": "1.2.4",
  "description": "Simple backend API for fetching dynamic theme configurations and UI states",
  "private": true,
  "author": "frontend-dev",
  "license": "ISC",
  "scripts": {
    "dev": "netlify dev",
    "build": "echo 'No build step required'"
  },
  "devDependencies": {
    "netlify-cli": "latest"
  }
}